import React from 'react';
import PropTypes from 'prop-types';

import ModalLineContainer from './ModalLineContainer';

const ModalLineIndex = (props) => {
  return(
    <ModalLineContainer />
  )
}

export default ModalLineIndex;
